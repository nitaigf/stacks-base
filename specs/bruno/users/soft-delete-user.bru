meta {
  name: Soft Delete User
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/users/{{createdUserId}}/soft-delete
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

assert {
  res.status: eq 200
  res.body.data.deletedAt: isString
}
