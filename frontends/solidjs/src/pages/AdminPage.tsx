export function AdminPage() {
  return (
    <section class="surface-card dashboard-card">
      <div class="page-header">
        <span class="metric-pill metric-pill-tag">Componente arquivado</span>
        <h2 class="form-title">AdminPage legado</h2>
        <p class="form-copy">
          Este componente de mock foi aposentado com a introducao das rotas administrativas reais.
          Use `/admin/users` e `/admin/audit-logs` no fluxo atual.
        </p>
      </div>
    </section>
  );
}
