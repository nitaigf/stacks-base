meta {
  name: Deactivate User
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/users/{{createdUserId}}/deactivate
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

assert {
  res.status: eq 200
  res.body.data.status: eq inactive
}
