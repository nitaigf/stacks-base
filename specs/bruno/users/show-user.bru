meta {
  name: Show User
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/users/{{createdUserId}}
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

assert {
  res.status: eq 200
  res.body.data.id: eq {{createdUserId}}
}
